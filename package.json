{
  "name": "ideam-sergiobc-website",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker app for ideam.sergiobc.com",
  "scripts": {
    "check": "node --check workers/ideam.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.14.1"
  }
}
